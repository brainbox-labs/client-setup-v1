export NODE_AUTH_TOKEN=

BRAINBOX_ORG=
BRAINBOX_PROJECT=
BRAINBOX_API_KEY=        # org-level key, starts with bb_
BRAINBOX_MCP_KEY=       # project-level key, starts with bb_proj_

DATABASE_URL=            # postgres://..., trino://..., or athena://<region>/<database>?catalog=...&workgroup=...&output=s3://...

# Only needed if DATABASE_URL is athena:// or awsathena://
export AWS_ACCESS_KEY_ID=
export AWS_SECRET_ACCESS_KEY=
